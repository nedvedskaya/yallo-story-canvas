/**
 * MinimalismLayout1 — стуб. Ждёт HTML-эталон от Ольги.
 *
 * Пока вёрстка не пришла, рендерим базовый Minimalism-контент (title/body/highlight
 * с format-aware размерами). Когда прилетит уникальный дизайн для layout 1,
 * содержимое этого файла переписывается на свой JSX; MinimalismBase продолжит
 * жить для остальных ещё-не-задизайненных layouts.
 */
import MinimalismBase from "./MinimalismBase";

export default MinimalismBase;
